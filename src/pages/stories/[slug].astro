---
import { storiesData } from '../../data/stories';
import { authors } from '../../data/authors';

export function getStaticPaths() {
  return storiesData.map((story) => ({
    params: { slug: story.slug },
    props: { story },
  }));
}

const { story } = Astro.props;
const author = authors.find(a => a.slug === story.authorSlug);
const authorName = author?.name || 'ToolStep Team';
const pageUrl = new URL(`/stories/${story.slug}/`, Astro.site).toString();
const publisherUrl = new URL('/', Astro.site).toString();
const storiesIndexUrl = new URL('/stories/', Astro.site).toString();

// Story metadata schema (Discover-friendly)
const storySchema = {
  '@context': 'https://schema.org',
  '@type': 'WebStory',
  headline: story.title,
  description: story.description,
  datePublished: story.datePublished,
  dateModified: story.datePublished,
  author: { '@type': 'Person', name: authorName },
  publisher: {
    '@type': 'Organization',
    name: 'ToolStep',
    logo: {
      '@type': 'ImageObject',
      url: story.publisherLogo,
    },
  },
  image: [story.poster, story.posterLandscape],
  mainEntityOfPage: { '@type': 'WebPage', '@id': pageUrl },
};

// Breadcrumb schema
const breadcrumbSchema = {
  '@context': 'https://schema.org',
  '@type': 'BreadcrumbList',
  itemListElement: [
    { '@type': 'ListItem', position: 1, name: 'Home', item: publisherUrl },
    { '@type': 'ListItem', position: 2, name: 'Stories', item: storiesIndexUrl },
    { '@type': 'ListItem', position: 3, name: story.title, item: pageUrl },
  ],
};
---

<!doctype html>
<html ⚡ lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,minimum-scale=1,initial-scale=1" />
    <title>{story.title} — ToolStep Web Stories</title>
    <meta name="description" content={story.description} />
    <link rel="canonical" href={pageUrl} />
    <link rel="amphtml" href={pageUrl} />

    {/* Open Graph + Twitter for share-friendly */}
    <meta property="og:title" content={story.title} />
    <meta property="og:description" content={story.description} />
    <meta property="og:image" content={story.poster} />
    <meta property="og:url" content={pageUrl} />
    <meta property="og:type" content="article" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={story.title} />
    <meta name="twitter:description" content={story.description} />
    <meta name="twitter:image" content={story.poster} />

    {/* AMP runtime */}
    <script is:inline async src="https://cdn.ampproject.org/v0.js"></script>
    <script is:inline async custom-element="amp-story" src="https://cdn.ampproject.org/v0/amp-story-1.0.js"></script>

    {/* Story metadata schema */}
    <script type="application/ld+json" set:html={JSON.stringify(storySchema)} />
    <script type="application/ld+json" set:html={JSON.stringify(breadcrumbSchema)} />

    {/* AMP boilerplate CSS */}
    <style is:inline amp-boilerplate>
      body {
        -webkit-animation: -amp-start 8s steps(1, end) 0s 1 normal both;
        -moz-animation: -amp-start 8s steps(1, end) 0s 1 normal both;
        -ms-animation: -amp-start 8s steps(1, end) 0s 1 normal both;
        animation: -amp-start 8s steps(1, end) 0s 1 normal both;
      }
      @-webkit-keyframes -amp-start {
        from { visibility: hidden; }
        to { visibility: visible; }
      }
      @-moz-keyframes -amp-start {
        from { visibility: hidden; }
        to { visibility: visible; }
      }
      @-ms-keyframes -amp-start {
        from { visibility: hidden; }
        to { visibility: visible; }
      }
      @-o-keyframes -amp-start {
        from { visibility: hidden; }
        to { visibility: visible; }
      }
      @keyframes -amp-start {
        from { visibility: hidden; }
        to { visibility: visible; }
      }
    </style>
    <noscript set:html={'<style amp-boilerplate>body{-webkit-animation:none;-moz-animation:none;-ms-animation:none;animation:none}</style>'} />

    {/* Custom styles — AMP-safe (no external fonts, no !important on layout) */}
    <style is:inline amp-custom>
      :root {
        --story-accent: {story.color};
      }
      body {
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
        margin: 0;
        padding: 0;
        background: #1a1a1a;
      }
      amp-story {
        background: #1a1a1a;
      }

      /* Cover slide */
      .cover-headline {
        font-size: 2.4rem;
        font-weight: 800;
        line-height: 1.15;
        color: #ffffff;
        text-shadow: 0 2px 12px rgba(0, 0, 0, 0.7);
        margin: 0 0 0.5rem;
      }
      .cover-subtext {
        font-size: 1.05rem;
        line-height: 1.5;
        color: rgba(255, 255, 255, 0.92);
        text-shadow: 0 1px 8px rgba(0, 0, 0, 0.6);
        margin: 0;
      }

      /* Content slide headlines */
      .slide-headline {
        font-size: 2rem;
        font-weight: 800;
        line-height: 1.2;
        color: #ffffff;
        text-shadow: 0 2px 12px rgba(0, 0, 0, 0.75);
        margin: 0 0 0.5rem;
      }
      .slide-subtext {
        font-size: 1.1rem;
        line-height: 1.5;
        color: rgba(255, 255, 255, 0.92);
        text-shadow: 0 1px 8px rgba(0, 0, 0, 0.65);
        font-weight: 500;
        margin: 0;
      }

      /* CTA slide */
      .cta-headline {
        font-size: 1.8rem;
        font-weight: 800;
        color: #ffffff;
        margin: 0 0 0.5rem;
        text-shadow: 0 2px 12px rgba(0, 0, 0, 0.7);
      }
      .cta-subtext {
        font-size: 1rem;
        color: rgba(255, 255, 255, 0.88);
        margin: 0 0 1.5rem;
        text-shadow: 0 1px 8px rgba(0, 0, 0, 0.5);
      }

      /* Gradient overlay layer (for readability) */
      .gradient-overlay {
        background: linear-gradient(
          180deg,
          rgba(0, 0, 0, 0.15) 0%,
          rgba(0, 0, 0, 0.0) 35%,
          rgba(0, 0, 0, 0.0) 55%,
          rgba(0, 0, 0, 0.7) 100%
        );
        height: 100%;
        width: 100%;
      }

      /* Category badge */
      .category-badge {
        display: inline-block;
        font-size: 0.72rem;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        padding: 4px 10px;
        border-radius: 6px;
        margin-bottom: 0.75rem;
        color: #ffffff;
        background: var(--story-accent);
      }

      /* Publisher / author footer text */
      .publisher-text {
        font-size: 0.75rem;
        color: rgba(255, 255, 255, 0.65);
        margin-top: 0.5rem;
        text-shadow: 0 1px 4px rgba(0, 0, 0, 0.5);
      }


      /* CTA button (amp-story-cta-layer) */
      .cta-button {
        display: inline-block;
        padding: 0.8rem 2rem;
        border-radius: 50px;
        background: var(--story-accent);
        color: #ffffff;
        font-weight: 700;
        font-size: 1rem;
        text-decoration: none;
        text-align: center;
        margin: 0 auto;
      }
    </style>
  </head>
  <body>
    <amp-story
      standalone=""
      title={story.title}
      publisher="ToolStep"
      publisher-logo-src={story.publisherLogo}
      poster-portrait-src={story.poster}
      poster-square-src={story.posterSquare}
      poster-landscape-src={story.posterLandscape}
    >
      {story.pages.map((page, i) => (
        <amp-story-page id={page.id}>
          <amp-story-grid-layer template="fill">
            <amp-img
              src={page.image}
              width="720"
              height="1280"
              layout="responsive"
            />
          </amp-story-grid-layer>
          <amp-story-grid-layer template="fill">
            <div class="gradient-overlay"></div>
          </amp-story-grid-layer>
          <amp-story-grid-layer template="vertical" class="text-layer">
            {i === 0 && (
              <>
                <span class="category-badge">{story.category}</span>
                <h1 class="cover-headline">{page.headline}</h1>
                {page.subtext && <p class="cover-subtext">{page.subtext}</p>}
                <p class="publisher-text">By {authorName} · ToolStep</p>
              </>
            )}
            {i > 0 && i < story.pages.length - 1 && (
              <>
                <h2 class="slide-headline">{page.headline}</h2>
                {page.subtext && <p class="slide-subtext">{page.subtext}</p>}
              </>
            )}
            {i === story.pages.length - 1 && (
              <>
                <h2 class="cta-headline">{page.headline}</h2>
                {page.subtext && <p class="cta-subtext">{page.subtext}</p>}
              </>
            )}
          </amp-story-grid-layer>
          {page.ctaLabel && page.ctaUrl && (
            <amp-story-cta-layer>
              <a class="cta-button" href={page.ctaUrl}>{page.ctaLabel}</a>
            </amp-story-cta-layer>
          )}
        </amp-story-page>
      ))}

      {/* Related stories */}
      {story.relatedStories.length > 0 && (
        <amp-story-page id="related">
          <amp-story-grid-layer template="fill">
            <amp-img
              src={story.posterLandscape}
              width="1280"
              height="720"
              layout="responsive"
            />
          </amp-story-grid-layer>
          <amp-story-grid-layer template="fill">
            <div class="gradient-overlay"></div>
          </amp-story-grid-layer>
          <amp-story-grid-layer template="vertical" class="text-layer">
            <h2 class="cta-headline">More Stories</h2>
            <p class="cta-subtext">Swipe to explore more from ToolStep</p>
          </amp-story-grid-layer>
          <amp-story-cta-layer>
            <a class="cta-button" href={storiesIndexUrl}>Browse All Stories</a>
          </amp-story-cta-layer>
        </amp-story-page>
      )}
    </amp-story>


  </body>
</html>
